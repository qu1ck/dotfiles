# Dotfiles

These are configs and scripts that are managed by chezmoi

Install with

```
sh -c "$(curl -fsLS get.chezmoi.io)" -- -b $HOME/.local/bin
```

Apply using

```
$HOME/.local/bin/chezmoi init qu1ck --apply
```

